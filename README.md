# 📈 주식 정보 조회

공공데이터포털 금융위원회 주식시세정보 API를 사용한 주식 정보 조회 서비스

## 주요 기능

- 📈 한국 주식 시세 조회 (일 1회 갱신)
- 📊 일간/주간/월간 차트 표시
- 🔍 종목 검색 (엔터키로 검색)
- 📑 여러 종목 탭으로 관리
- 💹 주요 지표 표시 (시가, 고가, 저가, 거래량, 등락률)
- 🤖 AI 기반 종목 분석 제공
- 🚀 전일 급등 종목 TOP 5 제공

> ⚠️ **주의**: 데이터 갱신 시간(매일 오전 09:00~10:00)에는 검색 서비스가 일시적으로 제한될 수 있습니다.

## 기술 스택

- **Frontend**: Vanilla JavaScript + Chart.js
- **Backend**: Cloudflare Workers + Hono
- **API**: 공공데이터포털 금융위원회 주식시세정보 API
- **TypeScript** - 타입 안정성

## 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 공공데이터포털 API 키 발급

1. [공공데이터포털](https://www.data.go.kr) 회원가입
2. [금융위원회_주식시세정보](https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15094808) 페이지 접속
3. "활용신청" 클릭하여 API 키 발급 신청 (승인까지 1~2시간 소요)
4. 승인 후 "마이페이지" → "오픈API" → "인증키"에서 키 확인
5. `.dev.vars` 파일에 API 키 입력:

```
DATA_GO_KR_API_KEY=your_api_key_here
```

### 3. 로컬 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:8787` 접속

## Cloudflare Pages 배포

### 1. Cloudflare 계정 로그인

```bash
npx wrangler login
```

### 2. Pages 프로젝트 생성 및 배포

```bash
npx wrangler pages project create ai-playlist
npx wrangler pages deploy --project-name=ai-playlist
```

### 3. 환경 변수 설정

Cloudflare 대시보드에서:
1. Workers & Pages → ai-playlist → Settings → Variables
2. `DATA_GO_KR_API_KEY` 추가

또는 CLI로:

```bash
npx wrangler secret put DATA_GO_KR_API_KEY
```

## 사용법

1. 검색창에 종목명 또는 종목코드 입력
   - 예: 삼성전자, 005930, SK하이닉스, 000660

2. 엔터키를 누르면 종목 정보 표시

3. 탭을 클릭하여 여러 종목 전환 가능

4. 차트 기간 버튼(일간/주간/월간)으로 차트 조회

## 지원 종목

현재 하드코딩된 한국 주요 종목들:
- 삼성전자 (005930)
- SK하이닉스 (000660)
- NAVER (035420)
- 카카오 (035720)
- 현대차 (005380)

더 많은 종목을 추가하려면 [src/index.ts](src/index.ts)의 `stockMap` 객체를 수정하세요.

**참고**: 공공데이터 API는 한국 주식만 지원하며, 데이터는 일 1회 갱신됩니다.

## 프로젝트 구조

```
.
├── public/          # 정적 파일
│   ├── index.html   # 메인 HTML
│   ├── styles.css   # 스타일시트
│   └── app.js       # 클라이언트 JavaScript
├── src/
│   └── index.ts     # API 서버 (Hono)
├── wrangler.toml    # Cloudflare Workers 설정
├── .dev.vars        # 환경 변수 (로컬 개발용)
└── package.json
```

## API 엔드포인트

### `GET /`, `/styles.css`, `/app.js`
정적 파일 서빙

### `POST /api/search`
종목 검색 API

**Request:**
```json
{
  "query": "삼성전자"
}
```

**Response:**
```json
{
  "code": "005930",
  "name": "삼성전자",
  "price": 71000,
  "change": 1000,
  "changeRate": 1.43,
  "open": 70500,
  "high": 71500,
  "low": 70000,
  "volume": 12345678
}
```

### `POST /api/chart`
차트 데이터 조회 API

**Request:**
```json
{
  "code": "005930",
  "period": "D"
}
```

**Response:**
```json
{
  "labels": ["11/01", "11/02", ...],
  "prices": [70000, 70500, ...]
}
```

## 라이선스

ISC
